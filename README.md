1. Var can change. Let can also change. Const cannot change.
2. map() returns a new array. forEach() does not return anything. filter() returns new array with items match and conditional match.
3. Arrow function short way to write function.
4. Destructuring mean taking value from objects or arrays and put them into valiables quickly.
5. Template literals → put variables or expressions in strings with ${}.
